# Núcleo da API
fastapi
uvicorn[standard]

# Testes
pytest

# Google Calendar (opcional — só precisa se for ligar a agenda real).
# Para usar, descomente as três linhas abaixo:
# google-api-python-client
# google-auth
# google-auth-oauthlib
